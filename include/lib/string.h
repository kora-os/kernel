#pragma once

#include "common.h"

// Freestanding memory primitives. The compiler emits calls to these for
// aggregate copies/initialisation and for constant-size byte loops even under
// -ffreestanding -fno-builtin, so the kernel must provide them.
void *memcpy(void *dest, const void *src, size_t n);
void *memmove(void *dest, const void *src, size_t n);
void *memset(void *dest, int c, size_t n);

int strlen(const char *str);
int strcmp(const char *str1, const char *str2);
int strncmp(const char *str1, const char *str2, size_t n);
char *strcpy(char *dest, const char *src);
char *strncpy(char *dest, const char *src, size_t n);
char *strcat(char *dest, const char *src);
char *strncat(char *dest, const char *src, size_t n);

